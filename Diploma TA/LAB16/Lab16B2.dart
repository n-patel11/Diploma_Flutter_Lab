// W.A.P. to navigate from one page to another page and pass data to
// another page using constructor. (B)


import 'package:flutter/material.dart';

class Lab16b2 extends StatelessWidget {
  const Lab16b2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Data naviget one page to another page using constructor"),),
      body: Row(
        children: [
          Center(
            child: Column(

            ),
          )
        ],
      ),
    );
  }
}
